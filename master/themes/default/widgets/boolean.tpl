<tr>
 <td colspan="2">
  <label for="{escape(param.name)}"><input type="checkbox" id="{escape(param.name)}" name="{escape(param.name)}"{if param.value}checked{endif} /> {escape(param.title)}</label>
 </td>
</tr>
